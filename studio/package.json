{
  "name": "treasurydirect-dashboard-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Studio for managing the TreasuryDirect dashboard content",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed": "node ../scripts/sanity-seed.mjs"
  },
  "devDependencies": {
    "@sanity/cli": "^3.50.0",
    "sanity": "^3.50.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "styled-components": "^6.5.1"
  }
}
